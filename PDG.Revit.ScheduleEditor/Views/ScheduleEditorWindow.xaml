<Window x:Class="PDG.Revit.ScheduleEditor.Views.ScheduleEditorWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:syncfusion="http://schemas.syncfusion.com/wpf"
        mc:Ignorable="d"
        Title="{Binding ScheduleName, StringFormat='Schedule Editor — {0}'}"
        Width="1000" Height="680"
        MinWidth="600" MinHeight="400"
        WindowStartupLocation="CenterOwner"
        ResizeMode="CanResize"
        ShowInTaskbar="False"
        Closing="Window_Closing">

    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis" />

        <Style x:Key="PrimaryButtonStyle" TargetType="Button">
            <Setter Property="Background"      Value="#0078D4" />
            <Setter Property="Foreground"      Value="White" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Padding"         Value="18,6" />
            <Setter Property="FontSize"        Value="13" />
            <Setter Property="Cursor"          Value="Hand" />
            <Style.Triggers>
                <Trigger Property="IsEnabled" Value="False">
                    <Setter Property="Background" Value="#BDBDBD" />
                </Trigger>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#005A9E" />
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="StatusBarStyle" TargetType="TextBlock">
            <Setter Property="Padding"     Value="8,4" />
            <Setter Property="FontSize"    Value="11" />
            <Setter Property="Foreground"  Value="#555555" />
            <Setter Property="TextWrapping" Value="Wrap" />
            <Setter Property="Background"  Value="#F5F5F5" />
        </Style>

        <Style x:Key="UnsavedDotStyle" TargetType="Ellipse">
            <Setter Property="Width"              Value="8" />
            <Setter Property="Height"             Value="8" />
            <Setter Property="Fill"               Value="#E67700" />
            <Setter Property="Margin"             Value="0,0,6,0" />
            <Setter Property="VerticalAlignment"  Value="Center" />
            <Setter Property="Visibility"
                    Value="{Binding HasUnsavedChanges, Converter={StaticResource BoolToVis}}" />
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />   <!-- Header bar -->
            <RowDefinition Height="*"    />   <!-- SfDataGrid -->
            <RowDefinition Height="Auto" />   <!-- Status bar  -->
            <RowDefinition Height="Auto" />   <!-- Button row  -->
        </Grid.RowDefinitions>

        <!-- ── Header bar ──────────────────────────────────────────── -->
        <Border Grid.Row="0"
                Background="#F0F0F0"
                BorderBrush="#CCCCCC"
                BorderThickness="0,0,0,1"
                Padding="12,8">
            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                <Ellipse Style="{StaticResource UnsavedDotStyle}" />
                <TextBlock Text="{Binding ScheduleName}"
                           FontSize="15"
                           FontWeight="SemiBold"
                           VerticalAlignment="Center" />
                <TextBlock Text=" — unsaved changes"
                           FontSize="12"
                           Foreground="#E67700"
                           VerticalAlignment="Center"
                           Visibility="{Binding HasUnsavedChanges, Converter={StaticResource BoolToVis}}" />
            </StackPanel>
        </Border>

        <!-- ── SfDataGrid ──────────────────────────────────────────── -->
        <!--
            Columns are generated at runtime from the DataTable columns via
            the AutoGeneratingColumn event in code-behind (ScheduleEditorWindow.xaml.cs).
            Read-only cells receive a light-grey background via QueryCellInfo.
        -->
        <syncfusion:SfDataGrid
            Grid.Row="1"
            x:Name="ScheduleGrid"
            ItemsSource="{Binding GridData}"
            AutoGenerateColumns="True"
            AllowEditing="True"
            NavigationMode="Cell"
            SelectionMode="Single"
            ShowGroupDropArea="False"
            AllowSorting="True"
            AllowFiltering="False"
            AllowResizingColumns="True"
            ColumnSizer="AutoLastColumnFill"
            Margin="0"
            AutoGeneratingColumn="ScheduleGrid_AutoGeneratingColumn" />

        <!-- ── Status bar ──────────────────────────────────────────── -->
        <TextBlock Grid.Row="2"
                   Text="{Binding LastApplyResult}"
                   Style="{StaticResource StatusBarStyle}"
                   MinHeight="24" />

        <!-- ── Button row ──────────────────────────────────────────── -->
        <Border Grid.Row="3"
                Background="#F8F8F8"
                BorderBrush="#DDDDDD"
                BorderThickness="0,1,0,0"
                Padding="12,8">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <!-- Hint text shown when there are unsaved changes -->
                <TextBlock Grid.Column="0"
                           Text="You have unsaved changes. Click Apply to write them to Revit."
                           FontSize="11"
                           Foreground="#E67700"
                           VerticalAlignment="Center"
                           Visibility="{Binding HasUnsavedChanges, Converter={StaticResource BoolToVis}}" />

                <StackPanel Grid.Column="1"
                            Orientation="Horizontal">
                    <Button Content="Apply"
                            Width="90"
                            Style="{StaticResource PrimaryButtonStyle}"
                            Command="{Binding ApplyCmd}"
                            Margin="0,0,8,0"
                            ToolTip="Write all changed cells back to Revit in a single transaction." />
                    <Button Content="Close"
                            Width="80"
                            Padding="16,6"
                            FontSize="13"
                            Command="{Binding CloseCmd}"
                            IsCancel="True"
                            ToolTip="Close the editor.  Unsaved changes will be discarded." />
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</Window>
