<Window x:Class="RoomDocumenter.UI.RoomPickerDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Room Documenter — Select Rooms for Interior Elevations"
        Height="560" Width="620"
        WindowStartupLocation="CenterOwner"
        ResizeMode="CanResizeWithGrip">

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <TextBlock Grid.Row="0"
                   Text="Select rooms for 4-way interior elevation creation."
                   FontStyle="Italic" Margin="0,0,0,6"/>

        <!-- Search box -->
        <Grid Grid.Row="1" Margin="0,0,0,6">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Column="0" Text="Search:" VerticalAlignment="Center" Margin="0,0,6,0"/>
            <TextBox   Grid.Column="1"
                       Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"/>
        </Grid>

        <!-- Room list -->
        <ListView Grid.Row="2"
                  ItemsSource="{Binding FilteredRooms}"
                  SelectionMode="Extended"
                  ScrollViewer.VerticalScrollBarVisibility="Auto">
            <ListView.View>
                <GridView>
                    <GridViewColumn Width="34">
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <CheckBox IsChecked="{Binding IsSelected, Mode=TwoWay,
                                          UpdateSourceTrigger=PropertyChanged}"
                                          HorizontalAlignment="Center"/>
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>
                    <GridViewColumn Header="Number" DisplayMemberBinding="{Binding Number}" Width="80"/>
                    <GridViewColumn Header="Name"   DisplayMemberBinding="{Binding Name}"   Width="240"/>
                    <GridViewColumn Header="Level"  DisplayMemberBinding="{Binding LevelName}" Width="160"/>
                </GridView>
            </ListView.View>
        </ListView>

        <!-- Select all / clear -->
        <StackPanel Grid.Row="3" Orientation="Horizontal" Margin="0,6,0,0">
            <Button Content="Select All"
                    Command="{Binding SelectAllCommand}"
                    Width="90" Height="26" Margin="0,0,8,0"/>
            <Button Content="Clear All"
                    Command="{Binding ClearAllCommand}"
                    Width="90" Height="26"/>
        </StackPanel>

        <!-- Crop offset -->
        <Grid Grid.Row="4" Margin="0,10,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="80"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Column="0" Text="Crop offset (mm):"
                       VerticalAlignment="Center" Margin="0,0,8,0"/>
            <TextBox   Grid.Column="1"
                       Text="{Binding CropOffsetMm, UpdateSourceTrigger=PropertyChanged}"
                       VerticalAlignment="Center"/>
            <TextBlock Grid.Column="2" Text="  (default 300 mm)"
                       VerticalAlignment="Center" Foreground="Gray" FontStyle="Italic"/>
        </Grid>

        <!-- Validation message + buttons -->
        <Grid Grid.Row="5" Margin="0,8,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <TextBlock Grid.Column="0"
                       Text="{Binding ValidationMessage}"
                       Foreground="Red"
                       VerticalAlignment="Center"
                       TextWrapping="Wrap"/>

            <Button Grid.Column="1"
                    Content="Create Elevations"
                    Command="{Binding ConfirmCommand}"
                    Width="130" Height="28" Margin="0,0,8,0"/>
            <Button Grid.Column="2"
                    Content="Cancel"
                    Command="{Binding CancelCommand}"
                    Width="80" Height="28"/>
        </Grid>
    </Grid>
</Window>
